/**
 * This is a submodule of the foo directory. It would be imported as `foo/helpers` and should be
 * treated as a distinct thing.
 * @param blah some string
 * @returns that string
 */
export function foo(blah: string) {
  return blah
}
